# EventView refactor — status (archive)

`EventView` was split into custom hooks (`useEventData`, `useCharacterManagement`, `usePartyManagement`) and smaller components. Optional items listed here (e.g. `WaitingRoomHeader`) may be outdated relative to the current `app/components/EventView` tree.

For an up-to-date description of data flow and hooks, see [ARCHITECTURE.md](./ARCHITECTURE.md).
