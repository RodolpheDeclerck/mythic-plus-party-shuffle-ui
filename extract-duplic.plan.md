<!-- Historical Cursor plan — superseded by current implementation -->
# EventView incremental refactor (archive)

This file described moving logic into `useEventData`, `useCharacterManagement`, and `usePartyManagement` in small steps. That extraction is reflected in the current codebase under `app/hooks/` and `app/components/EventView/`.

See [ARCHITECTURE.md](./ARCHITECTURE.md) for the current EventView flow.
